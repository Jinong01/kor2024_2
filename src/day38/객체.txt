[ 객체 ]
    - 자바의 객체지향 언어
        - 객체지향 : 자바 , 파이썬 , 자바스크립트 , C# 등등
    - 객체 Object :
        vs 주체 Subject : 객관에 대립되는 말로 주관을 가진 개체
        - 개체 : 하나의 형체
    -> 예] 개발자(주체/나)가 컴퓨터(프로그래밍)에서 형체(객체) 만들기
    생활 속 객체] 의자 , 컴퓨터 , 안경 , 스크린 , 공기 , 물 등등
    강사 입장에서 '유재석' 학생은 객체
    학생(유재석) 입장에서 강사는 객체

[ 자바 객체] ex]
    1. 클래스 기반의 메모리 공간
    2. 클래스란? 변수와 메소드를 정의하는 일종의 틀, 기본타입이 아닌 새로운 타입 생성
        -> 클래스 정의한다는 것은 새로운 타입을 만드는 것과 동일
    3. 클래스 멤버:
        1) 멤버변수/필드/속성/Key : 객체가 가지는 고유한 성질
        2) 생성자               : 객체가 생성될 때 초기화 함수
        3) 메소드/멤버함수        : 객체의 특정한 행동/명령어를 수행하는 공간/행위
    4. 객체란? 자바에서 클래스타입으로 생성된 메모리 공간
    5. 객체 생성 방법? new 생성자명()
        주의할점 : 객체는 변수가 참조하지 않으면 GC 에 의해 자동으로 객체 메모리는 제거된다.
            String str = new String(); // 변수 1개 , 객체 1개 (105번지)
            str = new String(); // 변수 1개 , 객체 1개 (106번지) , 105번지 객체가 사라졌다.

    6. 객체의 다형성
        - 다형성 : *하나의 객체가 여러가지 타입으로 변환 될 수 있는 성질 * 다양한 형태를 가질 수 있는 성질, 다양한 타입을 가질수 있는 성질
            - 캐스팅/변환 : 자동타입변환 , 강제타입변환 , 클래스(업/다운 캐스팅)
                - 자동타입변환 : byte -> short -> int -> long -> float -> double / 작은 것 -> 큰 것
                - 강제타입변환 : byte <- short <- int <- long <- float <- double / 큰 것 -> 작은 것
                - (상속) : 클래스의 멤버변수를 물려받는 문법 , 키워드 : extends , 물려받는 행위 (승계)
                    업캐스팅 : 하위(자식)타입이 상위(부모) 타입으로 변환
                    다운캐스팅 : 상위(부모)타입에서 하위(자식) 타입으로 변환 , 타입변환 객체가 본래 자식타입 이어야만 한다.
                    사용처 : 클래스의 멤버코드들이 중복일 때
                    DB 연동할 때, Connection 코드를 Dao 인터페이스로 만들고 나머지 Dao 들에게 상속하여 사용
                    하위클래스의 객체가 생성되면 상위클래스의 객체도 생성된다.(최종적으로 Object 까지)
                     ex] BoardDao extends Dao{}
                         new BoardDao() 하게 되면 총 객체는 1. BoardDao 객체 2. Dao 객체 3. Object 객체 - 3개
                         -> 눈에 보이지 않는 상위타입의 객체 항상 같이 만들어진다!!